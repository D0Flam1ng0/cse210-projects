using System;

class scripture
{
    Random rnd = new Random();
    bool running = true;
    while(running)
    {
        Console.WriteLine("Type quit in order to leave or press the enter key in order to contine");
        
    }
}
// Verse Ideas lets do a doctrinal mastery